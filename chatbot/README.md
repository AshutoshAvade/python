# Trained Models Package

This ZIP contains:
- Trained ETA regression model (RandomForest)
- Intent label artifacts for Transformer-based intent classifier

How to use:
1) Unzip into your project root
2) Point eta_utils.py to models/eta_model.pkl
3) (Optional) Fine-tune DistilBERT/BERT further using your intent_data.csv
